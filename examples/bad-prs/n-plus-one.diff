diff --git a/app/Http/Controllers/Api/TodoController.php b/app/Http/Controllers/Api/TodoController.php
index 1234567..abcdefg 100644
--- a/app/Http/Controllers/Api/TodoController.php
+++ b/app/Http/Controllers/Api/TodoController.php
@@ -22,9 +22,12 @@ final class TodoController extends Controller
     public function index(): AnonymousResourceCollection
     {
-        $todos = Todo::query()
-            ->where('user_id', auth()->id())
-            ->with('tags')
-            ->latest()
-            ->paginate(20);
+        $todos = Todo::query()
+            ->where('user_id', auth()->id())
+            ->latest()
+            ->paginate(20);
+
+        foreach ($todos as $todo) {
+            $todo->tag_names = $todo->tags->pluck('name')->toArray();
+        }

         return TodoResource::collection($todos);
     }
