# Eval set manifest
#
# Cada entrada é uma task-padrão usada para medir competência do agente.
# Categoria determina como pondera a pontuação geral.
#
# Vê examples/eval-tasks/ para exemplos completos de tasks.

version: 1
last_updated: "{{TODAY}}"

tasks:
  # Task semente: smoke test do harness (agnóstica ao domínio). Verifica que o
  # agente faz a coisa mais simples dentro das convenções. Adiciona mais à medida
  # que descobres áreas críticas. Categorias: crud, authorization, performance,
  # refactor, bug, adversarial.
  - id: harness-smoke-test
    category: crud
    description: "Endpoint GET /api/health-deep + componente Vue HealthBadge, dentro das convenções"
    difficulty: easy
    expected_duration_seconds: 300
    active: true

deprecated: []
